# Shared Type Definitions
Placeholder for common TypeScript interfaces, types, and enums used across the monorepo.
Currently, most shared types reside in `libs/shared/types.ts` or `libs/core/domain/shared/`.
